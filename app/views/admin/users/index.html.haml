.well
  %h6 Stats
  #{@users.count} total

= paginate @users

%table.table.table-striped.table-bordered
  %tr
    %th Email
    %th Failed Attempts
    %th Sign In Count
    %th Last Signed In

  - @users.each do |user|
    %tr
      %td
        = link_to user.email, admin_user_path(user)
        .actions
          = link_to '<i class="icon-trash"></i>'.html_safe, polymorphic_url([:admin, @project, user], format: :json), remote: true, confirm: 'Are you sure?', method: :delete, class: "btn delete", title: "Delete"
      %td= user.failed_attempts
      %td= user.sign_in_count
      %td= user.last_sign_in_at

= paginate @users