%p
  = link_to 'Back', admin_project_feeds_path(@project), title: "Back", class: "btn btn-inverse"

.row
  .span6
    = form_for [:admin, @project, @feed], html: { class: "form-horizontal"} do |f|
      - if @feed.errors.any?
        .alert.alert-error
          %p
            %b
              = "#{pluralize(@project.errors.count, "error")} prohibited this project from being saved:"
          %ul
            - @feed.errors.full_messages.each do |msg|
              %li= msg

      %fieldset
        %legend= controller_action
        .control-group
          = f.label :name, class: "control-label"
          .controls= f.text_field :name

        .control-group
          = f.label :url, class: "control-label"
          .controls= f.text_field :url

        .control-group
          = f.label :limit, class: "control-label"
          .controls= f.text_field :limit

        .control-group
          = f.label :rss, class: "control-label"
          .controls= f.text_field :rss
          
        .form-actions
          = f.hidden_field :project_id
          = f.submit 'Save', class: 'btn btn-primary'
