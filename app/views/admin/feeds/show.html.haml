- unless notice.blank?
  #notice.alert= notice

%p
  = link_to 'Back', admin_project_feeds_path(@project), title: "Back", class: "btn btn-inverse"
  = link_to '<i class="icon-plus"></i>'.html_safe, new_admin_project_feed_path(@project), title: "New", class: "btn"

%textarea{class: "tmpl", "data-project" => "#{@project.name}", "data-model" => "#{controller_name}", "data-action" => "#{action_name}"}
  %tr
    %td{colspan: 2}
      = link_to '<i class="icon-edit"></i>'.html_safe, admin_url + "${name}/edit", class: "btn", title: "Edit"
      = link_to '<i class="icon-trash"></i>'.html_safe, admin_url + "${name}", confirm: 'Are you sure?', method: :delete, class: "btn delete", title: "Delete"
  %tr
    %td Created At
    %td ${created_at}
  %tr
    %td Updated At
    %td ${updated_at}
  %tr
    %td Name
    %td ${name}
  %tr
    %td URL
    %td= link_to '${url}', '${url}'
  %tr
    %td Limit
    %td ${limit}
  %tr
    %td RSS
    %td {{each(i, j) rss}} <div class="well">{{each(a, b) j}}<strong>${a}</strong>: ${b}<br/>{{/each}}</div> {{/each}}

.row
  .span12
    %table.table.table-bordered