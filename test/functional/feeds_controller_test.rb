require 'minitest_helper'

describe FeedsController do
  before do
    @project = load_project
  end

  describe "on GET to :index" do
    it "returns a list of feed entries in json format" do
      skip
    end
  end
end